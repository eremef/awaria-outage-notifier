fix: Resolve blank screen on fresh install and add Stoen regression test

Maintainer Notes:
- Frontend: Replaced direct class manipulation with the `toggleSettings(false)` helper to ensure `main-view` is unhidden correctly after adding the first address. Leaving it hidden resulted in a blank/white screen on initial setup.
- Testing: Added a regression test for Stoen's address formatting to ensure the house number matcher properly captures multiple specific comma-separated numbers and ignores ones that are not explicitly listed.

Summary of Changes:
- `public/script.js`: Updated the save logic to use `toggleSettings(false)` instead of removing the `open` class from `settings-view`.
- `src-tauri/src/utils.rs`: Added a Stoen regression test for `match_house_number`.

User-facing:
- Fixed a bug where the app would show a blank white screen immediately after finishing initial setup.
